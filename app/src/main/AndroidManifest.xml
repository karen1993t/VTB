<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vtb.vtbproject">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VtbProject">
        <activity android:name=".open_vtb_card_steps.OpenVtbCardStepsActivity"
            android:theme="@style/action_bar_white"/>
        <activity android:name=".authorization.Authorization"
            android:theme="@style/themeAuth"/>
        <activity
            android:name=".on_boarding_about_fragments.OnBoardingAbout"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:screenOrientation="locked"/>
        <activity android:name=".create_account_and_visa_card.ShowAuthorizationActivity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>